'number' + 3 + 3
// Відповідь number33

null + 3
// Відповідь 3

5 && "qwerty"
// Відповідь qwerty

+'40' + +'2' + "hillel";
// Відповідь 42hillel
// 40 і 2 тут є числами, вони між собою додаються та конектяться зі словом hillel

'10' - 5 === 6;
// Відповідь false
// 10 перетворується на число, і при виконанні віднімання, не буде дорівнювати 6

true + false
// Відповідь 1
// true має значення 1

'4px' - 3
// Відповідь NaN
// '4px' не опредєляється як число бо в лапках є букви, тому віднімання не може бути виконане 

'4' - 3
// Відповідь 1
// '4' визначається числом і відбуваєтья віднімання

'6' + 3 ** 0;
// Відповідь 61
// 3 ** 0 дорівнює 1, потім "6" поєднується з 1

12 / '6'
// Відповідь 2
// відбувається ділення, '6' є числом

'10' + (5 === 6);
// Відповідь 10false
// '10' є числом, 5 не дорівнює 6 тому має значення false, 10 і false поєднуються разом

null == ''
// Відповідь false
// Значення null і undefined рівні == один одному і не рівні будь-якому іншому значенню.

3 ** (9 / 3);
// Відповідь 27
// 9 ділимо на 3 = 3, 3 у ступені 3 дорівнює 27

!!'false' == !!'true'
// Відповідь true

0 || '0' && 1
// Відповідь 1

(+null == false) < 1;
// Відповідь true
// null перетворюється на число і стає 0, при порівнянні з false є правдивим, а 0 є менше одиниці 

false && true || true
// Відповідь true
// Оператор && має вищий пріоритет за ||, тому відбувається порівняння true з true

false && (false || true);
// Відповідь false

(+null == false) < 1 ** 5;
// Відповідь true
// +null перетворується на число 0, при порівнянні з false видасть true, а потім відбудеться порівняння true < 1 в ступені 5